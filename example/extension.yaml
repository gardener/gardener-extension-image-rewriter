apiVersion: operator.gardener.cloud/v1alpha1
kind: Extension
metadata:
  annotations:
    security.gardener.cloud/pod-security-enforce: baseline
  name: extension-image-rewriter
spec:
  deployment:
    extension:
      helm:
        ociRepository:
          ref: europe-docker.pkg.dev/gardener-project/public/charts/gardener/extensions/image-rewriter:v0.5.0-dev
  resources:
  - kind: Extension
    lifecycle:
      reconcile: AfterKubeAPIServer
    type: image-rewriter
